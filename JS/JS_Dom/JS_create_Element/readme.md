<!-- Create Nodes -->
1: Create a Element
2: Create a Text node
3: Create a Attribute Node

<!--Acessing attributes-->
1 : getAttribute 
2 : setAttribute
3 : removeAttribute

<!-- Add Notes to the Dom -->

1: appendChild(node)
    Adds a node as the last child of the parent.
2: append(nodes1, nodes2, ....)
    Add several nodes of the parents
3: prepend and append of the nodes
    Add at first and the last of the nodes
4: insertBefore(value,node)
    Add before the specific nodes
5: Replace child(old_element, new_element)
    replace the node with new and older one 
6: using insertAdjacentHTML () or insertAdjacentElement():

beforebegin : Before the element itself.
afterbegin: Inside the element, before its first child.
beforeend: Inside the element, after its lastchild.
afterend: After the element itself.

8: removeChild(node)